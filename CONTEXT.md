# CONTEXT

이 프로젝트가 쓰는 말의 정의. 구현 이야기는 여기 적지 않는다 — 어디에 저장하는지,
어떤 테이블인지는 `docs/gitbook/`과 `docs/adr/`가 담당한다.

## 발전

**발전 실적 (Generation Record)**
개별 발전 설비가 한 시간 동안 실제로 생산한 **전력량**. 단위 kWh. 사업자 계량값에서
온다. 설비 단위라 발전소와 호기로 식별된다.

**계통 발전 구성 (Generation Mix)**
계통 전체가 특정 순간 각 연료원에서 받고 있는 **출력**. 단위 MW. 발전 실적을 요약한
값이 아니라 **다른 측정**이다. 항목에 따라 계량이 아니라 추정일 수 있다.

> 발전 실적과 계통 발전 구성을 더하거나 union 하지 않는다. 계통 발전 구성은 개별
> 발전 실적을 이미 포함하므로 합치면 이중 계산이고, 애초에 kWh 와 MW 라 단위가 다르다.

**계량값 / 추정값**
계량값은 설비에 붙은 계기가 읽은 값이다. 추정값은 계기가 없거나 계통에 안 잡히는
전력을 원천 기관이 모형으로 채운 값이다. 정산·검증에는 계량값만 쓴다.

**설비용량 (Capacity)**
설비가 낼 수 있는 최대 출력. MW. 발전 실적을 설비용량으로 나눈 것이 이용률이다.

**집합 설비 (Aggregate Plant)**
여러 설비의 합계를 한 행으로 내보내는 발전소. 개별 설비와 함께 합산하면 이중
계산이 된다.

## 시간

**구간시작 (Interval Start)**
`09:00` 이 `[09:00, 10:00)` 구간을 뜻하는 표기. 이 프로젝트가 내보내는 모든 시계열의
기본 규약이다.

**구간종료 (Interval End)**
`1~24시` 처럼 구간이 끝난 시각을 라벨로 쓰는 표기. 여러 원천이 이 방식을 쓰며,
적재할 때 구간시작으로 옮긴다.

**리드타임 (Lead Time)**
예보를 발표한 시각과 예보가 가리키는 시각의 차이. 예보에만 있고 관측에는 없다.

## 수요

**계통 수요 (System Demand)**
계통 전체가 특정 순간 소비하는 전력. MW.

**용도별 사용량 (Usage by Contract Type)**
계약 종별·업종별로 집계한 전력 사용량. 기간 단위 합계이며 순시 출력이 아니다.

## 전기차 충전

**충전 세션 (Charging Session)**
충전기 한 대에서 한 번 이뤄진 충전. 시작 시각, 종료 시각, 충전량(kWh)을 갖는다.
길이가 제각각이라 **그 자체로는 시계열이 아니다.**

**충전 부하 프로파일 (Charging Load Profile)**
고정 격자(5분·1시간)에 걸친 충전 전력 또는 전력량. 충전 세션의 충전량을 세션이
걸친 구간에 배분해 만든다. **원천에서 바로 오지 않는 파생물이다.**

**충전기 상태 스냅샷 (Charger Status Snapshot)**
일정 주기로 찍은 충전기의 상태 코드. 충전 중인 대수는 셀 수 있지만 **전력량은
알 수 없다.** 충전 부하 프로파일을 만들려면 충전기 용량을 곱한 추정에 그친다.

## 지역

**계통 단위 (System-wide)**
지역 구분 없이 계통 전체를 하나로 본 값.

**시도 단위 / 설비 단위**
광역시도로 나눈 값, 그리고 개별 설비 위치까지 내려간 값. 셋은 서로 다른 해상도이며
낮은 해상도에서 높은 해상도를 복원할 수 없다.
