-- ============================================================================
-- 발전소 좌표 수정 (Task 8) — plants.lat/lon
--
-- 근거: docs/plant-coordinates-audit.md
-- 주의: 이 파일은 실행되지 않았다. 컨트롤러 검토 후 결정.
--       모든 UPDATE는 plant_id를 명시하고, 이전 값을 주석에 남겨 되돌릴 수 있게 했다.
--       롤백하려면 각 UPDATE의 "이전 값" 주석대로 SET 하면 된다(마지막에 롤백 블록도 첨부).
-- ============================================================================


-- ─────────────────────────────────────────────────────────────────────────
-- 0. 실행 전 검증 — 대상 23기 현재 상태 스냅샷
-- ─────────────────────────────────────────────────────────────────────────
SELECT plant_id, plant_name, operator, lat, lon, address
FROM plants
WHERE plant_id IN (
    -- 기존 값 정정 (5)
    33, 26, 4, 11, 2,
    -- 신규 채움 (18)
    9, 8, 1, 12, 10, 13,      -- 신인천 계열
    5, 6, 7, 14, 15,          -- 부산 계열
    3,                        -- 남제주소내
    18, 16, 17,               -- 삼척 계열
    140, 141, 142             -- 영암 계열
)
ORDER BY plant_id;


-- ============================================================================
-- 1. 기존 값이 틀려서 정정 (5건) — §3, §4
-- ============================================================================

-- 1-1. 두산엔진MG태양광 (namdong) — §3
--   근거: 실사 주소 "경남 창원시 성산구 공단로 67(신촌동)". Nominatim으로 지오코딩한
--   "공단로"(창원국가산업단지) 좌표들과의 거리가 database.py 값(최근접 3.00km)이
--   init_db.py 값(최근접 5.30km)보다 일관되게 가깝다. database.py 값 채택.
--   이전 값(init_db.py 유래, 틀림): lat=35.2100, lon=128.5800
UPDATE plants SET lat = 35.2270, lon = 128.6811 WHERE plant_id = 33;

-- 1-2. 고흥만 수상태양광 (namdong) — §3
--   근거: 남동발전·한국농어촌공사 공동사업 공시 "전남 고흥군 두원면 학곡리" 소재
--   고흥호(96만㎡, 83MW)를 Nominatim으로 지오코딩. 기존 두 값(database.py/init_db.py)
--   모두 이 지점에서 5~10km 떨어져 있어 제3 값(공시 주소 지오코딩)으로 교체.
--   신뢰도: 근사 (리 단위 중심, 저수지 내 정밀 위치 아님)
--   이전 값(init_db.py 유래, 부정확): lat=34.5500, lon=127.2800
UPDATE plants SET lat = 34.6437, lon = 127.2575 WHERE plant_id = 26;

-- 1-3. 부산신항태양광 (nambu) — §4
--   근거: 현재 값(35.1796/129.0756)은 실제로는 "부산" 도시 대표좌표(부산시청)다.
--   pv_test/init_db.py의 geocode_address()가 발전소명에 "부산"이 부분 문자열로
--   포함된다는 이유로 실제 주소("부산 강서구 성북동 1490")를 지오코딩하지 않고
--   하드코딩 도시좌표를 채택한 버그. 주소를 직접 Nominatim으로 지오코딩해 교체.
--   신뢰도: 근사 (동 단위 중심, 지번 1490 정밀 위치 아님)
--   이전 값(부산시청 placeholder, 틀림): lat=35.1796, lon=129.0756
UPDATE plants SET lat = 35.0573, lon = 128.8102 WHERE plant_id = 4;

-- 1-4. 부산역 선상 주차장 (nambu) — §4
--   근거: 1-3과 동일한 버그(주소 "부산광역시 동구 초량3동 1187-1"를 두고
--   "부산" 도시좌표가 채택됨). 주소를 직접 Nominatim으로 지오코딩해 교체.
--   신뢰도: 근사 (지번 1187 인근 도로 중심)
--   이전 값(부산시청 placeholder, 틀림): lat=35.1796, lon=129.0756
UPDATE plants SET lat = 35.1156, lon = 129.0426 WHERE plant_id = 11;

-- 1-5. 이천D(백사면B) 태양광 (nambu) — §4
--   근거: 현재 값(35.9078/127.7669)은 geocode_address()가 지오코딩에 완전히
--   실패했을 때 쓰는 "한국 중심" 기본값이다. 실제 주소("경기도 이천시 백사면
--   도립리 141-1")를 Nominatim으로 지오코딩하면 161km 떨어진 지점(도립리, 경기
--   이천시)이 나온다 — 이번 조사에서 발견된 것 중 가장 큰 오차.
--   신뢰도: 근사 (리 단위 중심, 지번 141-1 정밀 위치 아님)
--   이전 값(지오코딩 실패 기본값, 틀림): lat=35.9078, lon=127.7669
UPDATE plants SET lat = 37.3406, lon = 127.4687 WHERE plant_id = 2;


-- ============================================================================
-- 2. 좌표 없음 → 부지(사이트) 단위 근사 채움 (18건) — §5
--    전부 발전사 공식 사업소 주소를 Nominatim으로 지오코딩한 근사값이다.
--    이전 값은 전부 NULL/NULL.
-- ============================================================================

-- 2-1. 신인천빛드림본부 부속 태양광 6기
--   근거: 남부발전 공식 주소 "인천 서구 장도로 57(청라동)"
UPDATE plants SET lat = 37.5464, lon = 126.6059
WHERE plant_id IN (9, 8, 1, 12, 10, 13);
--   9  = 한국남부발전(주)_신인천해수구취수구 태양광발전실적
--   8  = 한국남부발전(주)_신인천전망대 태양광발전실적
--   1  = 한국남부발전(주)_신인천소내 태양광발전실적
--   12 = 한국남부발전(주)_신인천 북측부지 태양광발전실적
--   10 = 한국남부발전(주)_신인천 주차장 태양광발전실적
--   13 = 한국남부발전(주)_신인천 1, 2단계 주차장 태양광발전실적

-- 2-2. 부산빛드림본부 부속 태양광 5기
--   근거: 남부발전 공식 주소 "부산 사하구 감천항로 7"
UPDATE plants SET lat = 35.0733, lon = 128.9905
WHERE plant_id IN (5, 6, 7, 14, 15);
--   5  = 한국남부발전(주)_부산복합자재창고 태양광발전실적
--   6  = 한국남부발전(주)_부산수처리장 태양광발전실적
--   7  = 한국남부발전(주)_부산운동장 태양광발전실적
--   14 = 한국남부발전(주)_부산본부 태양광발전실적 (1호기)
--   15 = 한국남부발전(주)_부산본부 태양광발전실적 (2호기)

-- 2-3. 남제주빛드림본부 부속 태양광 1기
--   근거: 남부발전 공식 주소 "제주 서귀포시 안덕면 화순해안로 106번길 55"
UPDATE plants SET lat = 33.2434, lon = 126.3390 WHERE plant_id = 3;
--   3 = 한국남부발전(주)_남제주소내 태양광발전실적

-- 2-4. 삼척빛드림본부 부속 태양광 3기
--   근거: 남부발전 공식 주소 "강원 삼척시 원덕읍 삼척로 734"
UPDATE plants SET lat = 37.2728, lon = 129.3229
WHERE plant_id IN (18, 16, 17);
--   18 = 한국남부발전(주)_삼척소내 태양광발전실적 (3호기)
--   16 = 한국남부발전(주)_삼척소내 태양광발전실적 (1호기)
--   17 = 한국남부발전(주)_삼척소내 태양광발전실적 (2호기)

-- 2-5. 한국농어촌공사 영암태양광 3기
--   근거: 디지털영암문화대전 "전남 영암군 삼호읍 용앙리" 간척지(2008년 준공, 3,000kW).
--   신뢰도: 근사 — 읍 단위 중심, "영암1차/영암2차"가 이 사업의 1·2단계인지는
--   문헌상 완전히 확인되지 않음(§5 참고). 셋 다 동일 좌표로 채움.
UPDATE plants SET lat = 34.7458, lon = 126.4694
WHERE plant_id IN (140, 141, 142);
--   140 = 영암태양광_합계
--   141 = 영암1차태양광
--   142 = 영암2차태양광

-- 2-6. 율치태양광 (plant_id=143) — 미확정, 의도적으로 UPDATE 없음.
--   웹 조회로도 위치를 특정할 근거를 찾지 못했다. 추측으로 채우지 않는다(§6).


-- ─────────────────────────────────────────────────────────────────────────
-- 3. 실행 후 검증 — 위 23건이 의도한 값으로 반영됐는지, 나머지(율치 등)는
--    그대로 NULL인지 확인
-- ─────────────────────────────────────────────────────────────────────────
SELECT plant_id, plant_name, operator, lat, lon,
       CASE WHEN lat IS NULL THEN '좌표없음' ELSE '좌표있음' END AS status
FROM plants
WHERE fuel_type = 'solar'
ORDER BY plant_id;

-- 좌표 없는 태양광이 정확히 1기(율치, plant_id=143)만 남아야 한다.
SELECT count(*) AS 좌표없는_태양광_기수
FROM plants WHERE fuel_type = 'solar' AND lat IS NULL;
-- 기대값: 1


-- ============================================================================
-- 4. 롤백 (필요 시 이 블록만 실행)
-- ============================================================================
-- UPDATE plants SET lat = 35.2100, lon = 128.5800 WHERE plant_id = 33;  -- 두산엔진MG태양광
-- UPDATE plants SET lat = 34.5500, lon = 127.2800 WHERE plant_id = 26;  -- 고흥만 수상태양광
-- UPDATE plants SET lat = 35.1796, lon = 129.0756 WHERE plant_id = 4;   -- 부산신항태양광
-- UPDATE plants SET lat = 35.1796, lon = 129.0756 WHERE plant_id = 11;  -- 부산역 선상 주차장
-- UPDATE plants SET lat = 35.9078, lon = 127.7669 WHERE plant_id = 2;   -- 이천D(백사면B) 태양광
-- UPDATE plants SET lat = NULL, lon = NULL WHERE plant_id IN (9,8,1,12,10,13,5,6,7,14,15,3,18,16,17,140,141,142);
