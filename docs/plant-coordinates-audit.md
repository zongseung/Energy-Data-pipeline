# 발전소 좌표 검증·감사 (Task 8)

작성일: 2026-08-11 · 대상: `plants.lat/lon` (태양광 45기) · 목적: `research` 배포 뷰(Task 3)가
발전량×기상(ASOS) 조인에 쓸 좌표를 확정한다. **운영 DB는 SELECT만 사용했다.**

## 0. 한 줄 결론

좌표 출처는 **2개**(`fetch_data/pv/database.py`의 `NAMDONG_PLANT_LOCATIONS`,
`pv_test/init_db.py`의 `PLANT_COORDS`) 뿐이다. 남동 태양광 23기 중 **21기가 두 소스 값이 다르다**
(2km 이내 사소한 차이 16기 + 결정적/유의미한 차이 5기). 조사 도중 **좌표를 가진 남부발전 3기가
자기 주소와 최대 161km 어긋나는 별도 버그**를 발견했고, 좌표가 없던 19기 중 18기를 발전사 공시
주소로 채웠다. **미확정은 `율치태양광` 1기뿐이다.**

| 범주 | 대상 | 판정 완료 | 미확정 |
|---|---|---|---|
| 남동 태양광 — 두 소스 값이 다름 | 21기 | **21** | 0 |
| 남동 태양광 — 두 소스 값이 같음 (구미·여수) | 2기 | 2 (불일치 아님) | — |
| 남부 태양광 — 좌표는 있는데 자기 주소와 어긋남 | 3기 | **3** | 0 |
| 태양광 — 좌표 전무 19기 | 19기 | **18** | **1** (`율치태양광`) |
| **합계 (이슈가 있던 plant_id)** | **43기** | **42** | **1** |

---

## 1. 소스 확인

좌표 하드코딩은 정확히 두 군데다. 셋째 후보로 지목된 `scripts/migrations/schema_migration.py`는
좌표를 **만들지 않는다** — `p2()`가 `nambu_plants`/`namdong_plants` 테이블(둘 다 `pv_test/init_db.py`가
CSV·지오코딩으로 채운 결과)을 조인하고, `namdong_plants`에 값이 없을 때만
`fetch_data.pv.database.get_namdong_location()`(=`database.py`의 사전)으로 폴백한다. 즉 지금
`plants.lat/lon`에 들어있는 값은 **전부 이 두 소스 중 하나에서 왔다.**

- `fetch_data/pv/database.py:145-190` — `NAMDONG_PLANT_LOCATIONS` (남동 11개 발전소명 → 좌표, 하드코딩)
- `pv_test/init_db.py:50-72` — `PLANT_COORDS` (남동 11개 + 남부 지역 8개 도시명 → 좌표, 하드코딩).
  이 파일의 `geocode_address()`가 **1순위로 이 하드코딩을 문자열 부분매칭**하고, 실패하면 Kakao→Nominatim
  API를 부른다. 남동 11개 발전소는 전부 이름이 정확히 일치해 하드코딩 값이 그대로 들어갔다
  (Kakao/Nominatim 호출 없음) — `plants` 현재 값과 대조해 확인.

## 2. 판정 방법

1. **행정구역 범위** — 발전소명의 지명이 좌표의 실제 행정구역과 맞는지 (역지오코딩 대신 OSM
   Nominatim으로 대상 주소를 지오코딩해 거리 비교. 새 의존성 추가 없음 — 1회성 웹 조회).
2. **같은 사이트 다른 설비 주소** — `plants`에서 같은 이름 계열의 비-태양광 설비(예: `영흥_4` 화력,
   `삼천포해양소수력_1`)가 주소를 갖고 있으면 그 좌표와 대조. **가장 강한 근거.**
3. **발전사 공시자료** — 남부발전 각 발전본부(신인천/부산/남제주/삼척)의 공식 주소, `(주)탑선`의
   자체 실적 목록, 남동발전 보도자료(고흥만 수상태양광) 등. 출처는 표마다 명시.
4. 위 어느 것도 없으면 **미확정**으로 남긴다.

거리는 모두 haversine으로 계산했다(파이썬 표준 `math`만 사용, 신규 의존성 없음).

---

## 3. 남동 태양광 23기 — `database.py` vs `init_db.py` 전수비교

| plant_id | plant_name | 현재 DB 좌표 | database.py | init_db.py | 차이 | 판정 | 근거 | 조치 |
|---|---|---|---|---|---|---|---|---|
| 39 | 구미태양광 | 36.1195/128.3446 | 36.1195/128.3446 | 36.1195/128.3446 | 0km | 동일(불일치 아님) | — | 없음 |
| 23 | 여수태양광 | 34.7604/127.6622 | 34.7604/127.6622 | 34.7604/127.6622 | 0km | 동일(불일치 아님) | — | 없음 |
| 41 | 예천태양광 | 36.6469/128.4375 | 36.6579/128.4526 | 36.6469/128.4375 | 1.82km | 실질적 동일(경북 예천군 내) | 두 값 모두 예천군 행정구역 범위 안 | 없음 |
| 38 | 경상대태양광 | 35.1582/128.1002 | 35.1535/128.0986 | 35.1582/128.1002 | 0.54km | 실질적 동일 | 두 값 모두 진주시 범위 안 | 없음 |
| 19 | 광양항세방태양광 | 34.9156/127.68 | 34.9126/127.6958 | 34.9156/127.68 | 1.48km | 실질적 동일(광양항 권역) | 두 값 모두 광양항 인근 | 없음 |
| 24,40,20,29,32,34 | 영흥태양광(6기) | 37.2594/126.4281 | 37.2542/126.4278 | 37.2594/126.4281 | 0.58km | 실질적 동일 — `database.py` 값이 사이트 주소와 정확 일치 | **`영흥_1~6`(화력)·`영흥해양소수력_1~3`·`영흥풍력 1~2` 주소 "인천 옹진군 영흥면 영흥로 293" = 37.2542/126.4278과 `database.py` 완전 일치.** 현재 DB 값도 580m 이내로 오차 무시 가능 | 없음 (영향 미미) |
| 31,36,22,28,27,21,37 | 삼천포태양광(7기) | 34.9454/128.068 | 34.9294/128.0656 | 34.9454/128.068 | 1.79km | 실질적 동일 — `database.py` 값이 사이트 주소와 정확 일치 | **`삼천포_3~6`(화력)·`삼천포해양소수력_1`·`삼천포풍력 1` 주소 "경남 고성군 하이면 하이로 1156-31" = 34.9294/128.0656과 `database.py` 완전 일치.** 현재 DB 값도 1.79km 이내 | 없음 (영향 미미) |
| 25 | 영동태양광 | **37.7519/128.8761** | **37.1837/128.9437** | **37.7519/128.8761** | **63.46km** | **`init_db.py`(=현재 DB)가 정확, `database.py`가 오류** | `영동_1/영동_2`(화력) 주소 "강원 강릉시 강동면 헌화로 1289-43"(영동에코발전본부) = 37.7447/128.9949. 이 지점까지 거리: `init_db.py` 값 **10.48km** vs `database.py` 값 **62.54km**. `database.py`는 강릉이 아니라 강원도 삼척 인근 좌표를 갖고 있다 | 없음 — 현재 DB가 이미 정답. `database.py` 오류는 §7에서 별도 제안 |
| 30, 35 | 탑선태양광_1·_3 | **35.2733/126.7297** | **35.9078/128.8097** | **35.2733/126.7297** | **200.87km** | **`init_db.py`(=현재 DB)가 정확, `database.py`가 오류** | "탑선"은 지명이 아니라 EPC/시행사 **㈜탑선**(topsun.kr)의 상호. 탑선 자체 실적 목록(topsun.kr 갤러리, 135건)이 전남 영광·신안·해남 등 **전남 서남부**에 집중. `init_db.py` 값(35.2733N/126.7297E)은 전남 장성/영광 인접 지역으로 이와 부합. `database.py` 값(35.9078N/128.8097E)은 **경북** 지역으로 탑선 실적 지역과 무관 | 없음 — 현재 DB가 이미 정답. `database.py` 오류는 §7에서 별도 제안 |
| 33 | 두산엔진MG태양광 | **35.21/128.58** | **35.227/128.6811** | **35.21/128.58** | **9.38km** | **`database.py`가 정확, `init_db.py`(=현재 DB)가 오류 → 수정 필요** | 실사 주소 "경남 창원시 성산구 공단로 67(신촌동)"(옛 두산엔진→HSD엔진→현 한화엔진). Nominatim으로 "공단로"(창원국가산업단지) 3개 지점을 지오코딩해 거리 비교: `database.py` 값 최근접 **3.00km**/평균 4.03km, `init_db.py` 값 최근접 **5.30km**/평균 7.69km — `database.py`가 일관되게 더 가깝다. `init_db.py`의 경도(128.58)는 창원산단 서쪽 경계 밖(함안 방면)이다 | **SQL 포함 — DB를 `database.py` 값으로 교체** |
| 26 | 고흥만 수상태양광 | 34.55/127.28 | 34.6047/127.285 | 34.55/127.28 | 6.10km | **제3 값(공시자료) 채택** — 두 소스 모두 부정확 | 남동발전·한국농어촌공사 공동사업 공시: "전남 고흥군 **두원면 학곡리**" 소재 고흥호(96만㎡, 83MW). 학곡리를 Nominatim으로 지오코딩 → **34.6437/127.2575**. 두 기존 값 모두 학곡리 중심에서 5~10km 떨어져 있었다(`database.py` 5.01km, `init_db.py` 10.62km) | **SQL 포함 — 학곡리 좌표로 교체(근사, 저수지 내 정밀 위치 아님)** |

**요약**: 23기 중 2기 동일, 16기 실질적 동일(<2km, 조치 불요), 3기(영동·탑선×2)는 현재 DB가
이미 정답, **2기(두산엔진MG·고흥만)는 SQL로 수정 필요**.

---

## 4. 부수 발견 — 좌표는 있지만 자기 주소와 어긋나는 남부발전 3기

태양광 45기 중 `address`가 채워진 건 3기뿐이다(부산신항태양광, 부산역 선상 주차장,
이천D(백사면B) 태양광). **셋 다 좌표가 자기 주소와 크게 어긋난다.** 원인은
`pv_test/init_db.py`의 `geocode_address()`가 **주소로 지오코딩하기 전에 발전소명을 하드코딩
사전에 부분 문자열로 먼저 매칭**하기 때문이다(`normalize_plant_name(key) in base_name` 검사).
"부산신항태양광"이라는 이름에 "부산"이 부분 문자열로 들어있어 `PLANT_COORDS["부산"]`
(=부산시청 좌표)이 실제 주소 지오코딩보다 먼저 채택됐다.

| plant_id | plant_name | 현재 DB 좌표 | 실제 주소 | 좌표의 정체 | 주소 지오코딩(Nominatim) | 차이 | 판정 | 조치 |
|---|---|---|---|---|---|---|---|---|
| 4 | 부산신항태양광 | 35.1796/129.0756 | 부산 강서구 성북동 1490 | **부산시청** 좌표(도시명 "부산" 부분매칭 오적용) | 35.0573/128.8102 (성북동, 가덕도동, 강서구) | 27.71km | 주소 지오코딩값 채택 | **SQL 포함** |
| 11 | 부산역 선상 주차장 | 35.1796/129.0756 | 부산광역시 동구 초량3동 1187-1 | **부산시청** 좌표(동일 버그) | 35.1156/129.0426 (1187-21, 중앙대로214번길, 초량동, 동구) | 7.72km | 주소 지오코딩값 채택 | **SQL 포함** |
| 2 | 이천D(백사면B) 태양광 | 35.9078/127.7669 | 경기도 이천시 백사면 도립리 141-1 | `geocode_address()` **전체 지오코딩 실패 시 기본값**("한국 중심") | 37.3406/127.4687 (도립리, 백사면, 이천시) | **161.53km** | 주소 지오코딩값 채택 | **SQL 포함** |

`schema_migration.py:169-174`는 `nambu_plants.latitude IS NOT NULL`이면 무조건
`capacity_confidence='확실'`로 표시한다 — 이 3기는 값이 있다는 이유만으로 "확실" 등급을
받았지만 실제로는 셋 다 틀렸다. **좌표 존재 여부와 정확도는 별개**라는 것을 이번 조사가
증명한다.

---

## 5. 좌표가 아예 없던 19기

`operator='nambu'` 15기(발전본부 부속시설명), `operator='ekr'` 4기(영암/율치)가 대상이다.
전부 `address`도 비어 있어 DB 내부 대조가 불가능했다(브리프 §제약). 발전사 공식 사업소 소개
페이지·공시자료를 웹 조회해 **부지(사이트) 단위 근사 좌표**를 채웠다 — 소형 옥상/주차장/취수구
태양광이 모(母) 발전소 부지 안에 있다는 전제다(§2 근거우선순위 #2와 동일 논리, 남부발전은
비태양광 자매설비가 DB에 없어 대신 발전사 공식 주소를 썼다).

| plant_id | plant_name | 소속 부지 | 근거(공식 주소) | 지오코딩 좌표 | 신뢰도 |
|---|---|---|---|---|---|
| 9, 8, 1, 12, 10, 13 | 신인천해수구취수구·신인천전망대·신인천소내·신인천 북측부지·신인천 주차장·신인천 1,2단계 주차장 | 신인천빛드림본부 | 남부발전 공식: "인천 서구 장도로 57(청라동)" | 37.5464/126.6059 | 근사 |
| 5, 6, 7, 14, 15 | 부산복합자재창고·부산수처리장·부산운동장·부산본부(1·2호기) | 부산빛드림본부 | 남부발전 공식: "부산 사하구 감천항로 7" | 35.0733/128.9905 | 근사 |
| 3 | 남제주소내 | 남제주빛드림본부 | 남부발전 공식: "제주 서귀포시 안덕면 화순해안로 106번길 55" | 33.2434/126.339 | 근사 |
| 18, 16, 17 | 삼척소내(1·2·3호기) | 삼척빛드림본부 | 남부발전 공식: "강원 삼척시 원덕읍 삼척로 734" | 37.2728/129.3229 | 근사 |
| 141, 142, 140 | 영암1차태양광·영암2차태양광·영암태양광_합계 | 한국농어촌공사 영암지사 | 디지털영암문화대전: "전남 영암군 삼호읍 용앙리" 간척지(2008년 준공, 3,000kW). "영암1차/2차"가 이 사업의 1·2단계인지 문헌상 완전히 확인되지는 않으나, odcloud 데이터셋 제목 자체가 "한국농어촌공사_영암 태양광 발전소 발전량 현황"이라 동일 사업으로 판단 | 34.7458/126.4694 (삼호읍 중심) | 근사 — 읍 단위 중심, 리 단위 정밀 위치 아님 |
| **143** | **율치태양광** | 불명 | 웹 조회로 "율치" 관련 저수지/부지를 특정하지 못함. odcloud 데이터셋명(15005796)도 "영암 태양광…"으로만 되어 있어 위치 단서가 없음 | — | **미확정** |

---

## 6. 미확정으로 남긴 것

**`율치태양광` (plant_id=143) 1기뿐이다.** "영암"과 짝지어 수집되는 이름이라 인근일 개연성은
있으나, place-name으로도 공시자료로도 위치를 특정할 근거를 찾지 못했다. 추측으로 채우지 않고
`lat/lon = NULL`로 남긴다. (참고: `fetch_data/pv/ekr_collect.py`는 발전량만 수집하고
좌표/주소 정보를 다루지 않으므로 수집기 코드에도 단서가 없다.)

---

## 7. 수집기 좌표 소스 통합 제안 (이 태스크에서는 실행하지 않음)

이번 조사로 확인된 구조적 문제. **코드는 건드리지 않았다** — 다음은 제안이다.

1. **SSOT 부재.** 좌표가 `fetch_data/pv/database.py`(하드코딩 dict)와 `pv_test/init_db.py`
   (하드코딩 dict + 지오코딩)에 중복 정의돼 있고, 서로 다른 값을 갖는다(§3). `plants` 테이블 자체를
   좌표 SSOT로 승격하고, 두 수집기가 이 테이블에서 조회하도록 리팩터링을 제안한다.
2. **부분 문자열 매칭 버그.** `pv_test/init_db.py:geocode_address()`와
   `fetch_data/pv/database.py:get_namdong_location()` 둘 다 발전소명에 사전 키가
   **부분 문자열로만** 포함되면 매칭된다(`if key in plant_name`). "부산"이 "부산신항태양광"에
   부분 매칭되어 실제 주소를 두고 도시 대표좌표를 쓴 것이 §4의 근본 원인이다. 정확히 일치하는
   키가 없으면 지오코딩을 우선하도록 순서를 바꿔야 한다.
3. **매칭 실패 시 무언(silent) 폴백.** `get_namdong_location()`은 매칭 실패 시 **서울시청 좌표**로,
   `geocode_address()`는 **한국 중심 좌표**(35.9078/127.7669)로 조용히 폴백한다(§4의
   `이천D(백사면B) 태양광`이 실제로 이 경로를 탔다). 폴백 대신 `NULL` + 경고 로그로 바꿔야
   틀린 값이 "확실"로 둔갑하는 사고(§4)가 재발하지 않는다.
4. **`capacity_confidence`의 의미 과다 사용.** `schema_migration.py`가 이 컬럼을 "좌표 존재 여부"로도
   쓰고 있는데(§4), 좌표 신뢰도와 용량 신뢰도는 다른 축이다. 좌표 전용 신뢰도 컬럼을 별도로 두는
   편이 낫다.

---

## 8. `sql/fix_plant_coordinates.sql` 요약

23건 `UPDATE`(기존 오류 정정 6건 + 신규 채움 17건) + 실행 전후 검증 쿼리. **실행하지 않았다** —
컨트롤러 검토 후 결정.

| 구분 | plant_id | 건수 |
|---|---|---|
| 기존 값이 틀려서 정정 | 33, 26, 4, 11, 2 | 5 |
| 좌표 없음 → 신규 채움 | 9,8,1,12,10,13,5,6,7,14,15,3,18,16,17,140,141,142 | 18 |
| 미확정 (SQL 없음) | 143 | — |

---

## 9. 셀프 리뷰

- **완전성**: 태양광 45기 전부를 다뤘다 — 남동 23기 두 소스 전수비교(§3), 남부 좌표보유 3기의
  자체 주소 대조(§4), 좌표 전무 19기의 보완 시도(§5).
- **정직성**: 근거 없이 판정하지 않았다 — `율치태양광`은 웹 조회로도 위치를 특정하지 못해
  미확정으로 남겼다. `고흥만 수상태양광`·`영암` 3기는 리/읍 단위 근사임을 명시했다(정밀 지번
  아님).
- **되돌림 가능**: SQL은 `plant_id`를 명시하고, 모든 `UPDATE`에 이전 값을 주석으로 남겼다.
- **절제**: 산출물은 이 문서와 `sql/fix_plant_coordinates.sql` 2개뿐이다. 수집기 코드는
  수정하지 않았다(§7은 제안일 뿐). 지오코딩 파이프라인을 만들지 않고 웹 조회 1회성 호출로
  마쳤다(신규 의존성 없음).
- **우려사항**: (1) 두산엔진MG·고흥만·신규 채움 18기는 전부 "근사" 신뢰도다 — 정확한 지번
  지오코딩이 아니라 행정구역/부지 주소 중심점이다. ASOS 조인에는 충분하지만 그 이상의 정밀도가
  필요하면 재작업이 필요하다. (2) "영암1차/영암2차"가 실제로 삼호읍 용앙리 사업의 1·2단계인지는
  완전히 확인하지 못했다 — 셋 다 우선 동일 좌표로 채웠다. (3) §7 제안은 이번 태스크 범위 밖이며
  별도 태스크로 진행해야 한다.
